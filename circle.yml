machine:
  environment:
 
    IMPORT_PATH: "github.com/erick-ochieng/app"
     
dependencies:
  pre:
    - go get -u /erick-ochieng/app

  override:
    - mkdir -p /home/erick/app
    - rsync -avC /home/erick/app
test:
    pre:
    - go build -o myapp .

