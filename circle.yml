machine:
  node:
    version: 8.9.4
  services:
   - docker

dependencies:
  post:
    - docker build -t erick .
test:
  post:
    - docker run -d -p 8080:8080 app; sleep 10
    
- curl --retry 10 --retry-delay 5 localhost:8080 | grep "Hello World, This is my Simple Web App"
