machine:
  services:
    - docker

dependencies:
  pre:
    - CGO_ENABLED=0 go build -a -installsuffix nocgo .
  override:
    - docker build -t quay.io/erick-ochieng/go-docker-circleci .
    - docker run -p 8080:8080 quay.io/erick-ochieng/go-docker-circleci:
        background: true
    - sleep 10

test:
  override:
    - "curl http://localhost:8080/"
    - "[[ $(curl http://localhost:8080/) = 'Hello World!' ]]"

deployment:
  hub: 
    branch: master
    commands:
- docker push quay.io/erick-ochieng/go-docker-circleci
