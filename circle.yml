machine:
  environment:
    IMPORT_PATH: git clone https://github.com/erick-ochieng/app.git

dependencies:
  pre:
    - go get github.com/erick-ochieng/app

  override:
    - mkdir -p /home/erick/app/myapp
    - rsync -azC rsync -avC /home/erick/app

test:
  pre:
    - go run server.go


