machine:
  environment:
 
    PROJECT_GOPATH: /home/erick/app
     
dependencies:
  pre:
    - go get -u github.com/erick-ochieng/app

  override:
    - mkdir -p /home/erick/app
    - rsync -avC /home/erick/app
test:
    pre:
    - go build -o myapp .

