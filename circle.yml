machine:
  environment:
    IMPORT_PATH: github.com/erick-ochieng/app/blob/master/circle.yml

dependencies:
  pre:
    - go get github.com/home/erick/app

  override:
    - mkdir -p /home/erick/app
    - rsync -azC rsync -avC /home/erick/app

test:
  pre:
    - go run server.go

