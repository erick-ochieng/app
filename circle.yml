machine:
  environment:
    IMPORT_PATH: /home/erick/app/github.com/erick-ochieng/app

dependencies:
  pre:
    - go get github.com/erick-ochieng

  override:
    - mkdir -p /home/erick/app/myapp
    - rsync -azC rsync -avC /home/erick/app

test:
  pre:
    - go run server.go

