machine:
  services:
    - docker

dependencies:
  pre:
    - go get github.com/erick-ochieng

  override:
    - chown root.root -R ../app
    - docker build -t server.go .
    - docker run -it -p 8080:8080 server.go
    
  test:
  override:
    - "curl http://localhost:8080/"
    - "[[ $(curl http://localhost:8080/) = 'Hello World, This is my Simple Web App' ]]"

deployment:
  hub: 
    branch: master




