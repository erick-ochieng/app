machine:
  environment:
    IMPORT_PATH: https://github.com/erick-ochieng/app.git

dependencies:
  pre:
    - go get -u github.com/erick-ochieng

  override:
    - mkdir -p /home/erick/app/myapp
    - rsync -azC rsync -avC /home/erick/app

test:
  pre:
    - go run server.go



